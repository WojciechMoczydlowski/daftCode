#include "DataControler.h"

DataControler::DataControler()
{
}

void DataControler::read_data(list<Ship> &ship_set)
{

    cout << ship_set[0];

    // fstream file;
    // file.open("dane.csv");

    // string row;
    // string line, element, temp;

    // while (file >> temp)
    // {
    //     first_row.clear();

    //     getline(file, line);
    //     stringstream s(line);

    //     while (getline(s, element, ';'))
    //     {
    //         first_row.push_back(element); //push_back() inserts an element at the end of the vector and vector's is size++
    //     }
    // }
}